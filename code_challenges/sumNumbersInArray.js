/**
 * 
 * @param {Array} numarray  of numbers
 * @returns Number
 * 
 * function to sum an array of numbers
 */
const sumNumbersInArray = numarray => numarray.reduce((acc,num) => acc + num)

export default sumNumbersInArray
